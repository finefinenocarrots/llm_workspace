# 2026-08-21 工作日志

## 关键词库&否词库增加点击复制功能 + 数据更新

### 代码改动 (keywords.js + keywords.html)
1. **keywords.js** `renderTable` / `renderTableBody` / `renderNegLib`: 关键词 `<td>` 增加 `kw-name` class（复用 common.css 已有的 kw-name 样式）
2. **keywords.js** 全局 click handler: 点击 `tr[data-kw]` 行时调用 `navigator.clipboard.writeText(kwName)` 复制关键词，并显示 `copy-flag` "已复制" 徽章 + `copied` 绿色高亮（1.2s 后移除）
3. **keywords.html** footnote 更新: "点击关键词行查看该词各匹配方式详情并复制关键词"
4. **keywords.js** 否词库 footnote 更新: "点击关键词复制到剪贴板"

### 数据更新
- kw_data.js: 3618KB, 69954 行，日期 2026-06-01 ~ 2026-08-19
- tg_data.js: 510KB, daily 10962 行 (2026-03-01 ~ 08-19), targets 695 行
- 版本号: `?v=20260821d1`（3个HTML共13处）
- commit: `169d5b6`，SSH push 成功

## 板块五匹配方式增加CVR转化率数据

### 代码改动 (ads.js + index.html)
1. **ads.js** `renderMatch`: 数据 map 增加 `cvr: v.cl > 0 ? v.od / v.cl : 0`
2. **ads.js** 图表: 新增 CVR 折线系列（diamond标记, 紫色#8a7aa8, 虚线, 次轴yAxisIndex:1），tooltip formatter 增加 CVR 百分比展示
3. **ads.js** 表格: thead 增加 `<th>CVR</th>`，tbody 行末增加 `<td>${F.pct(x.cvr, 1)}</td>`
4. **index.html** 图表标题: "各匹配方式：花费 / 曝光 / 点击 / ACoS / CVR"
- commit: `090e4ca`，SSH push 成功
