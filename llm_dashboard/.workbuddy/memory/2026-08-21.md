# 2026-08-21 工作日志

## 关键词库&否词库增加点击复制功能 + 数据更新

### 代码改动 (keywords.js + keywords.html)
1. **keywords.js** `renderTable` / `renderTableBody` / `renderNegLib`: 关键词 `<td>` 增加 `kw-name` class（复用 common.css 已有的 kw-name 样式）
2. **keywords.js** 全局 click handler: 点击 `tr[data-kw]` 行时调用 `navigator.clipboard.writeText(kwName)` 复制关键词，并显示 `copy-flag` "已复制" 徽章 + `copied` 绿色高亮（1.2s 后移除）
3. **keywords.html** footnote 更新: "点击关键词行查看该词各匹配方式详情并复制关键词"
4. **keywords.js** 否词库 footnote 更新: "点击关键词复制到剪贴板"

### 数据更新
- kw_data.js: 3618KB, 69954 行，日期 2026-06-01 ~ 2026-08-19
- tg_data.js: 510KB, daily 10962 行 (2026-03-01 ~ 08-19), targets 695 行
- 版本号: `?v=20260821d1`（3个HTML共13处）
- commit: `169d5b6`，SSH push 成功

## 板块五匹配方式增加CVR转化率数据

### 代码改动 (ads.js + index.html)
1. **ads.js** `renderMatch`: 数据 map 增加 `cvr: v.cl > 0 ? v.od / v.cl : 0`
2. **ads.js** 图表: 新增 CVR 折线系列（diamond标记, 紫色#8a7aa8, 虚线, 次轴yAxisIndex:1），tooltip formatter 增加 CVR 百分比展示
3. **ads.js** 表格: thead 增加 `<th>CVR</th>`，tbody 行末增加 `<td>${F.pct(x.cvr, 1)}</td>`
4. **index.html** 图表标题: "各匹配方式：花费 / 曝光 / 点击 / ACoS / CVR"
- commit: `090e4ca`，SSH push 成功

## 新增板块七「各类目星期表现对比」+ 有效状态筛选迁移

### 代码改动 (ads.js + index.html)
1. **ads.js** 顶部新增 `WEEK` / `dateWk`(日期→星期预计算)、`WK_METRICS`(6指标: 花费/广告订单/CTR/CPC/CVR/ACoS, dir 标注优劣方向)、`wkMetric='cvr'`
2. **ads.js** `buildWkSeg()`: 用 `.seg` 分段控件渲染 6 个指标按钮（点击只重渲染热力表）
3. **ads.js** `renderWeekday(cur)`: 聚合 类目×星期 (im/cl/sp/sa/od)，存 `_wkAgg`，调用预算建议 + 表格
4. **ads.js** `renderWkBudget(m)`: 按星期整体 CVR/ACoS 评分 → 效率排名 / 预算再平衡(仅当低效天占预算>高效天) / 周末vs工作日 三项建议
5. **ads.js** `renderWeekdayTable()`: 类目×星期 热力表，单元格按指标相对优劣着色(绿=优/红=差；花费/订单用中性蓝)，含"周均"列；重置按钮复位指标为 CVR
6. **index.html** 顶部筛选栏删除"有效状态"；板块六卡片内新增"有效状态"筛选(标注全局) + 提示文案；插入板块七(预算建议卡 + 对比表 + 分段控件)；原板块七重编号为板块八
- ⚠️ 调试发现 `renderWkBudget` 的 info 对象漏写 `cl` 字段导致 filter `d.cl>0` 恒 false → 一律"样本不足"；已补 `cl: o.cl` 修复
- 无头浏览器验证通过(10类目×9列, 6按钮, 67着色单元格, 指标切换正常, 无 console error 除 favicon 404)
- commit: `ae1ad8e`，SSH push 成功（纯 UI 改动，未 bump 版本号，需强制刷新）

## 板块七色块增强 + 周日置末 + 目标看板模块二费比环比 + 国家下移（统一推送）

### 代码改动
1. **ads.js** 板块七色块对比增强: 中值差异 1.35 倍放大；透明度 0.16~0.72；绿端点 `46,138,101` / 红端点 `201,62,55`；花费/订单中性蓝 0.12~0.58
2. **ads.js** 星期列顺序: 新增 `WK_ORDER = [1,2,3,4,5,6,0]`，表头与单元格按此渲染 → 列序 类目|周一~周六|周日|周均（周日记末尾）；预算建议的周末判定(d.w===0||6)不受影响
3. **target.html + target.js** 模块二: 【负责人】【国家】表各新增 3 列 费比·昨日/上周/4周（实际 TACOS 环比, pp；▲红▼绿）；国家卡片下移负责人下方并改整行宽；hint 说明
   - 口径锚点 `mE=effEnd`=数据最新日(现 8/19)；昨日=单日vs前一日；上周=近7天vs前7天；4周=近28天vs前28天(滚动4周over4周, 非"本周vs4周前单周")
4. **版本号** bump 至 `?v=20260821d2`（13处引用）
- 无头浏览器验证通过(表头顺序正确, 两表10列, 国家下移, 环比着色, 无 console error 除 favicon 404)
- commit: `69b9dc7`，SSH push 成功（4a3cde5..69b9dc7），已还原 HTTPS
