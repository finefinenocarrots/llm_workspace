# 2026-07-25 工作日志

## 跨境电商广告数据与目标达成看板（新建）

- 数据源：`resource/关键词报告-每日明细.xlsx`(sheet1, 11534行, 2026-06-26~07-25, 仅SP类型)；`resource/广告目标达成进度.xlsx`(产品表现数据源64117行日级 + 每月销售目标含"本月销售额目标/本月目标月花费" + list-info映射店铺)
- 关键口径：月目标花费取「每月销售目标.本月目标月花费」（每月广告目标.本月目标花费是日级口径，勿用）；目标TACOS=目标花费/目标销售额；国家中文名需映射为码(美国→US等)
- 处理脚本：`scripts/process_data.py` → 生成 `dashboard/data/kw_data.js`(571KB, 字典编码行) 与 `dashboard/data/tg_data.js`(430KB, daily聚合到日期×负责人×店铺×国家×类目 + targets月度)
- 前端：纯静态 HTML + 本地 echarts.min.js；`dashboard/index.html`(广告数据看板6板块) + `dashboard/target.html`(目标达成5模块)；公共组件在 `assets/common.js`(MultiSelect多选下拉/DateRange日期预设控件/CH图表主题)
- 验证方式：python -m http.server 8765 + puppeteer-core 驱动本机 Edge 无头截图检查（scripts/verify_pages.js）
- Python venv: `C:/Users/fang_hu/.workbuddy/binaries/python/envs/default/Scripts/python.exe` (含 pandas 3.0.5/openpyxl)

## 15:05 追加
- 用户反馈调整：目标达成看板模块一 KPI 前两卡改为「实际值在上加粗（k-value 700），目标值在下正常字重（k-delta）」，文件 dashboard/assets/target.js renderKpis()。

## 15:07 追加
- 用户反馈调整：目标达成看板模块二「月度费比(TACOS)完成趋势」纵轴改为动态范围（取两线极值 ±15% 边距，下限不低于0），renderMonthTrends() in dashboard/assets/target.js。

## 15:13 追加
- 用户反馈调整：目标达成看板日期筛选去掉「近7天/近14天/近30天」预设，仅保留 本月/上月/全部（target.js DateRange presets 改为 ['tm','lm','all']）。

## 15:18 追加
- 修复目标达成看板日期范围与数据不联动问题：renderAll 重构为按「所选范围覆盖的全部月份」聚合（mList），新增 filterTgtM(mlist,ds)；目标=覆盖月份目标之和，实际=首月月初~截止日；时间进度=已过天数/覆盖月总天数；renderCatRate/buildRank 改为多月目标聚合；归因对比改为「本期 vs 上一同长度周期」（表头带具体日期）；KPI 标签去掉「月」前缀。无头浏览器回归验证 0 错误。

## 15:26 追加
- 用户反馈调整：目标达成看板模块一新增两张 KPI 卡：「实际 ACOS」(adsp/adsa，副文字为广告销售额)与「实际 CPC」(adsp/clk，副文字为点击量)，插在实际TACOS与花费完成率之间，renderKpis() in dashboard/assets/target.js。

## 15:47 追加
- 汇率换算（仅广告数据看板生效）：关键词报告-每日明细.xlsx 新增「汇率」sheet（列：国家/本币=1/美元）。process_data.py Part1 读取该 sheet 构建 FX 映射（DE/FR/IT/ES=1.176, UK=1.358, CA=0.73, US=1.0），按每行国家把本币金额列(CPC/花费/广告销售额/直接销售额/间接销售额/CPA/广告笔单价-本币)×汇率换算成美元；比率列(ACoS/ROAS/CVR/CTR)不随汇率变化不处理。缺失国家会 raise 报错。重新生成 kw_data.js；目标看板 tg_data 不受影响。
- 校验：scripts/verify_fx.py 抽查各国 raw 本币合计×汇率 与生成美元合计吻合（微差来自逐行 r2 四舍五入）；无头浏览器两页 0 控制台错误。
- 环境备注：本地静态服务器端口 8765 曾遗留僵尸进程导致 ERR_EMPTY_RESPONSE，需强制结束进程后改用 8770；verify_pages.js 已支持 BASE_URL 环境变量。

## 15:55 追加
- 用户反馈"数据没变"：排查确认磁盘 kw_data.js(15:43) 已是美元口径（Node 聚合验证 CA 花费=11625.86、UK=7890.59），服务器返回与磁盘 MD5 一致，判定为浏览器缓存旧数据文件。修复：index.html/target.html 的 data 与 js 引用统一追加 ?v=20260725fx 版本参数破缓存；同时恢复 8765 端口服务，8765/8770 双端口均可访问。经验：改数据文件后要给静态资源加版本号防缓存。

## 16:00 追加
- 全部金额统一保留 2 位小数：common.js F.money() 默认小数位由「≥1000 取 0 位」改为始终 2 位（影响所有 KPI/表格/tooltip）。图表纵轴刻度保留 $Xk 缩写不动。target.js 月度趋势/14天趋势的花费系列喂图数据由 .toFixed(0) 改为 .toFixed(2)，使 tooltip 显示真实分位。版本号升 ?v=20260725fx2。无头验证两页 0 错误。

## 16:02 追加
- 广告数据看板两处调整（ads.js + index.html）：
  1) 模块一「ACoS 与 CPC 趋势」由双折线改为组合图——CPC 改柱状(次轴$)、ACoS 保平滑折线(主轴%含面积+30%基准线)，视觉区分两种量纲；ACoS 纵轴动态范围(极值±20%边距，下限≥0)使波动更明显；tooltip 加 cross 十字指示。renderTrends() chTrendAcos 块。
  2) 模块二删除「各店铺广告花费、销售额、ACoS、费比对比」图表(ch-shop)：移除 index.html 卡片、chShop 实例与 setOption 块，renderShop() 仅保留店铺明细表。板块二布局改为直接 g-2 双列(国家花费产出图 + 店铺明细表)。
- 版本号升 ?v=20260725fx3。无头验证 index 6 canvas(原7)、0 错误。

## 16:10 追加：整体 UI 配色重设计（去 AI 风格）
- 用户反馈配色太"AI 风格"（紫蓝渐变）。整体改为编辑风/商务风：
  - 背景 #f4f3ee 暖纸色，卡片白+1px 暖灰描边(#e7e4dc)+浅阴影，圆角 14→10px
  - 顶栏由紫蓝渐变改为深墨蓝 #24303d 纯色 + 赭黄 #d99a3d 下边线；sec-title 竖条改赭黄
  - 主色 #4f6ef7→墨蓝 #33608c；图表色板改低饱和莫兰迪 10 色（墨蓝/青绿/赭黄/灰紫/砖红/橄榄…），定义在 common.js CH.colors
  - 批量替换脚本 scripts/recolor.py + recolor2.py（hex 映射 + rgba 分量映射），仪表盘进度渐变改纯色
  - tag 圆角 20→4px，数字用 tabular-nums；版本号升 ?v=20260725ui4（common.css 也加了版本参数）
- 经验：改配色用映射表批量替换脚本，避免遗漏 rgba/colorStops 里的旧色。

## 16:19 追加
- 用户反馈调整：广告数据看板负责人表、店铺明细表去掉「费比、花费占比」两列，改为 CVR(od/cl)、CPC(sp/cl)、CTR(cl/im) 三列；index.html 负责人表标题/脚注同步更新口径说明。ads.js renderOwnerTable/renderShop（renderShop 删除无用的 const total）。版本号升 ?v=20260725ui6。无头验证两页 0 错误。

## 16:11 追加：浪费花费口径调整
- 广告看板板块五「浪费花费 Top20」新增门槛：累计点击 <15 次的词暂不纳入浪费（样本量不足）。ads.js renderKeywordTables() waste 过滤条件加 x.cl>=15；建议文案分支改为 ≥30 次直接暂停 / 15~29 次降竞价观察；index.html 表头 hint 同步注明口径。板块六汇总统计口径未动。版本号 ?v=20260725ui5。

## 16:23 追加：表格首列冻结
- 两个看板全部 .tbl 表格首列冻结（不随横向滚动）：common.css 给 .tbl td:first-child / th:first-child 加 position:sticky; left:0，白色不透明背景 + inset 右侧分隔线；z-index 层级 th首列3 > th2 > td首列1；hover 行首列背景同步 #f7f6f1。版本号统一升至 ?v=20260725ui7。

## 16:25 追加：右上角导航按钮优化
- 去掉导航文案里的两个 emoji 图标（📊/🎯），index.html 与 target.html 顶部 nav 文案改为纯文字「广告数据看板 / 目标达成看板」。
- 导航样式由普通文字链接重做为深色顶栏上的「分段控件」：父容器加 rgba(.07) 半透明底 + 1px 半透白描边 + 11px 圆角胶囊；非选中态半透明白字(.68)，hover 提亮(.12)；选中态改为白色实心胶囊(#fff + 墨蓝文字 + 阴影)，比旧版 inset 下边框更清爽。修改位置 common.css .topbar nav 块。版本号升 ?v=20260725ui8。无头验证两页 0 控制台错误。
