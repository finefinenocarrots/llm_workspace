# 2026-08-03 工作日志

## 执行 /update-dashboard-data 全量流程（成功）
- 源表 resource/ 两个 xlsx 于 08-03 10:32/10:05 更新（新增 8 月初数据）
- process_data.py（新版：金额直接美元，无汇率表，含 st 有效状态维度）重新生成：
  - kw_data.js：22703 行，2026-07-03~2026-08-01，1159 KB
  - tg_data.js：daily 9839 行(03-01~08-01) + targets 557 行，456 KB
- bump_data_version.py 升版本号 -> ?v=20260803d1（index/target 各 4 处）
- 无头浏览器回归验证通过：index 0 error/5KPI/9canvas；target 0 error/8KPI/7canvas
  - 排查坑：后台静态服务必须用 `--directory D:/workspace/llm_dashboard/dashboard` 显式指定根目录，
    否则 http.server 从错误目录起服导致全部 404（8765 端口疑有残留进程，改用 8799 成功）
- 本地提交 82622b0（10 文件，ahead 1）；**git push 在沙箱失败**：无 GitHub 凭证
  （credential.helper=helper-selector，无存储 token），需用户在本地终端 `git push origin main`

## 待办/注意
- 推送后用 GitHub Pages 部署：workflow 路径过滤命中 llm_dashboard/dashboard/**，会自动触发
- 前提：仓库 Settings→Pages Source 必须已设为 "GitHub Actions"（之前排查中用户尚未确认完成）；
  若未开启，deploy-pages 会报 "Ensure GitHub Pages has been enabled"
