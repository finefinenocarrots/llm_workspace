# 2026-07-26 工作日志

## 新增项目级 SKILL：update-dashboard-data（看板数据更新）
- 用户要求在 `D:\workspace\llm_dashboard` 下生成 SKILL，用于「源 Excel 追加行 → 重新生成前端数据 → 破除缓存」的闭环。
- 创建位置：`D:\workspace\llm_dashboard\.workbuddy\skills\update-dashboard-data\`
  - `SKILL.md`：标准流程（确认表结构 → 跑 process_data.py → 校验 → 升缓存版本号 → 无头验证 → 汇报），含常见坑位（新增国家必填汇率、目标数据日期下限 2026-03-01、比率列不随汇率变、维度自动扩展）。
  - `references/schema.md`：两个 Excel 的 sheet/列结构、汇率换算、目标口径、生成产物字段、追加行自检清单。
- 新增辅助脚本 `scripts/bump_data_version.py`：把 index.html/target.html 中所有 `?v=...` 引用统一替换为 `YYYYMMDDdN` 格式新版本（状态文件 scripts/.cachever 记录当日计数），强制浏览器刷新数据。
- 验证：process_data.py 已实跑成功（kw 11534行/572KB、tg daily 9270行+targets 557行/430KB，0 报错）；bump 脚本因当前无数据变更未实跑（避免无意义升版本号），逻辑已审阅。
- 数据流：resource/*.xlsx → scripts/process_data.py → dashboard/data/kw_data.js(kw) + tg_data.js(tg) → 前端 ?v= 强刷。

## 19:57 实跑 update-dashboard-data 流程（用户 /update-dashboard-data 更新数据）
- 跑了完整闭环：process_data.py 重新生成（kw 11534行/572KB、tg daily 9270+targets 557/430KB，0 报错）→ bump_data_version.py 升版本至 `?v=20260726d1`（index/target 各 4 处，共 8）→ 无头验证 0 控制台错误（index 4KPI+6canvas、target 8KPI+6canvas）。
- 重要发现：源 Excel 最后修改时间为 07-25（本次用户未实际追加新行），但 dashboard/data/*.js 是今日 19:49 才生成的；而 HTML 仍引用旧 `?v=20260725ui8` → 浏览器会读 07-25 的旧缓存。本次升版本号正是把 19:49 已生成的新数据推到浏览器。已如实告知用户：若需追加新行，改 Excel 后重跑即可。
- 端口事故处理：8765/8770 残留僵尸服务器（空响应/连接拒绝，属上一会话命名空间，本会话 taskkill/Stop-Process 均报"进程不存在"无法杀）。临时起 8780 验证通过；后 8770 变 REFUSED（僵尸已死），于本会话在 8770 起重启干净服务，最终在 8770 验证通过。结论：标准访问地址 http://localhost:8770 现已正常；8780 也保留可用。
- 经验：本地静态服务器跨会话会残留且本会话杀不掉；遇到空响应/拒绝时，换端口起新服务最稳。verify_pages.js 的 CHROME_PATH 必须用 Windows 盘符路径（C:/...），MSYS 的 /c/... puppeteer 解析不到。
