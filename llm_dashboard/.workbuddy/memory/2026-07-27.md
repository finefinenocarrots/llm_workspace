# 2026-07-27 工作日志

## 删除源表列 + 加固 update-dashboard-data 技能
- 用户要求：删除 `resource/关键词报告-每日明细.xlsx`(sheet1) 的 `间接销售额-本币` 列，并确保删列后仍可正常生成看板。
- 执行：
  1) 用 openpyxl 在 sheet1 删除该列（1-based index 24，33→32 列，11534 行数据保留）；其余 sheet(广告组合匹配/listing_info/汇率)与格式不动。该列仅出现在 sheet1。
  2) `scripts/process_data.py` 的 `local_money_cols` 移除 `间接销售额-本币`（原已有 `if c in df.columns` 容错，删列也不会崩；移除仅为与真实 schema 一致）。
  3) 更新技能文档：
     - `references/schema.md`：从金额类本币列清单删除该列，新增「删除列的安全规则」——可选金额列可删（脚本容错自动跳过），仅 花费-本币/广告销售额-本币 进前端输出；必需列（日期/曝光量/点击/花费-本币/广告销售额-本币/广告订单/广告销量 + 各维度列）不可删。
     - `SKILL.md` 常见坑位：新增「删除列是安全的（仅限可选金额列）」「同步脚本与文档」两条。
- 验证：process_data.py 重新生成 0 报错（kw 11534行/572KB、tg daily 9270+targets 557/430KB，行数与删列前一致，因该列未进输出）；bump 至 `?v=20260727d1`；无头验证 0 控制台错误（index 4KPI+6canvas、target 8KPI+6canvas）。8781 端口起新服务验证通过。
- 结论：删列对看板无影响（该列前端未用），技能已可在「删可选金额列」场景下正常生成。

## 11:17 追加行：补充 SB(Sponsored Brands) 数据，数据源翻倍
- 用户补充了 SB 类型数据，执行 /update-dashboard-data 更新流程。
- 数据源变化：sheet1 行数 11534 → 21817（约翻倍）；`类型` 维度新增 `SB`（原仅 `SP`）；6 个国家(CA/DE/ES/FR/UK/US)不变、汇率 sheet 已覆盖，无新增 FX 需求；店铺(17)/负责人(7)/广告组合(82) 均自动编入维度字典，无需改前端。
- process_data.py 重新生成 0 报错：kw_data.js 21817行/1062KB（日期 2026-06-26~07-25），tg_data.js daily 9393+targets 557/436KB（日级 2026-03-01~07-25）。
- bump 至 `?v=20260727d2`；无头验证 0 控制台错误（8782 端口，index 4KPI+6canvas、target 8KPI+6canvas）。
- 同步技能文档 `references/schema.md`：`类型` 维度说明更新为含 SB/SP，注明 2026-07-27 起数据源含 SB 行数翻倍。
- 端口：本会话起 8782 临时服务验证；标准 8770 上个会话曾重启，本次未重启（用户按需强刷）。
