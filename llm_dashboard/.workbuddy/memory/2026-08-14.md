# 2026-08-14 工作日志

## 10:12 更新数据源并推送（/update-dashboard-data）
- 用户补充数据源（SB 数据继续并入），执行完整更新+推送流程。
- 数据源：Excel 今日 08-14 修改；上次生成数据 08-12，需重算。
- process_data.py 重新生成 0 报错：
  - kw_data.js：63231 行（上次 21817，约 3 倍），**日期扩展到 2026-06-01 ~ 2026-08-12**（上次 06-26~07-25），3236 KB。
  - tg_data.js：日级 10526 行 + 月度目标 695 行（上次 9393/557），491 KB；日级 2026-03-01~08-12。
- bump 版本：`scripts/bump_data_version.py` 升到 `?v=20260814d1`，覆盖 index/target/**keywords**(共13处引用，比上次多一个 keywords.html)。
- 无头验证 0 控制台错误（8790 端口）：index 5KPI+9canvas、target 8KPI+7canvas、keywords.html 200。
- 提交：`94904b3`「2026-08-14: 数据刷新(SB数据并入, kw 63K行 日期06-01~08-12)+bump ?v=20260814d1」(8 files, 本地提交成功)。
- **推送踩坑（重要）**：本环境 `github.com:443` HTTPS 被防火墙拦截（api.github.com / codeload.github.com 可达，但 git 的 github.com:443 TCP 连接 21s 超时，连试 7 次均失败）；`ssh -T git@github.com` 却能认证成功 → **改用 SSH 推送** `git remote set-url origin git@github.com:finefinenocarrots/llm_workspace.git` 后 `git push` 成功（be84b13..94904b3）。
- 澄清：本仓库**并未真正启用 Git LFS**——无 `.gitattributes`，`git cat-file` 显示 kw_data.js 是完整 3.3MB blob（非 pointer）。之前 `git config filter.lfs.smudge` 有值只是全局 git-lfs 安装残留，未作用于任何文件。故数据文件随 SSH push 直接传全量，无 LFS 上传隐患。
- 推送后已把 remote 还原为 HTTPS（尊重用户原配置）。
- 部署核查：GitHub API 查 actions/runs，run `31763530781` 对 commit 94904b3 **completed/success** → GitHub Pages 已上线新数据。

## 结论 / 给未来的提醒
- 本环境推 GitHub **必须用 SSH**（HTTPS 的 github.com:443 被拦）。下次 /update-dashboard-data 推送若 HTTPS 失败，直接切 SSH：`git remote set-url origin git@github.com:finefinenocarrots/llm_workspace.git && git push origin main`，完事再还原 HTTPS。
- 仓库无 LFS，数据文件走普通 git 对象，SSH push 即全量上传。
- 远程核查用 `https://api.github.com/repos/finefinenocarrots/llm_workspace/actions/runs?per_page=3`（api.github.com 在本环境可达）。
