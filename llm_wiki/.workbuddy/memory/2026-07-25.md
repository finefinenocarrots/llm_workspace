# 2026-07-25

## 跨境电商 LLM WIKI 雏形（Karpathy 三层架构）
- 在 `D:\workspace\llm_wiki` 搭建纯 Obsidian 的 Karpathy 式 LLM Wiki（用户要求**不做 web 预览**）。
- 三层：L1 `00-Raw-Sources/`（原始资料，不可变，LLM 只读）；
  L2 `01-Wiki/`（LLM 拥有的知识库，按页面类型分目录：00-索引(MOC)/10-概念/20-实体/30-对比/40-综述/50-摘要）；
  L3 `02-Schema/CLAUDE.md`（规则手册 = Karpathy 的 CLAUDE.md）。
- 已填充：L3 规则、L1 README+1 示例文章(`articles/2026-07-25-跨境出海平台格局.md`)+本地下载占位图(`images/平台格局示意图.png`)、`模板/` 5 类模板、L2 约 15 篇互链笔记（首页+4 MOC+概念×4+实体×2+对比×2+综述×1+摘要×1）。
- 约定：Obsidian 原生 `[[wikilinks]]` 互链 + 文末 `## 相关`；frontmatter(type/domain/tags/updated/source)；新增 L1 资料走 CLAUDE.md 的 Processing 流水线（写摘要→级联更新 10–15 页→矛盾标注 `> [!warning] 数据冲突`→更新 MOC）。
- `.obsidian` 已设 `attachmentFolderPath=00-Raw-Sources/images`、`templatesFolder=模板`；templates/标签面板/图谱插件已启用。
- 领域范围（用户确认）：仅跨境电商；本轮四个子领域=平台运营/选品与供应链/物流与海外仓/营销与广告，其余子主题(合规税务/支付收款/数据分析)留作后续扩展。

## 知识库维护 SKILL + commands（原生可调用）
- 用户要求生成 SKILL 与 commands 执行三类操作：①摘录新内容纳入知识库 ②检索知识库 ③校验矛盾/防腐。
- 落地（按用户选的"仅原生可调用"）：工作区内 `D:\workspace\llm_wiki\.workbuddy\skills\llm-wiki-kb\SKILL.md`（主技能=SKILL 目录）+ `D:\workspace\llm_wiki\.workbuddy\commands\{ingest,search,validate}.md`（三个命令）。
- **关键坑**：本环境的 Skill 工具只发现 **user-level** `~/.workbuddy/skills/`，不发现 project-level 的 `.workbuddy/skills/`。因此又往 `C:\Users\fang_hu\.workbuddy\skills\` 安装了可调用副本：`llm-wiki-kb`(主) + `llm-wiki-ingest`/`llm-wiki-search`/`llm-wiki-validate`(三操作)，已验证可被 Skill 工具加载。
- 调用方式：用户说"摘录/纳入"→走 ingest；"检索/查找"→search；"校验/矛盾/防腐"→validate；或直接说用 llm-wiki-kb。
- 注意：workspace 的 `commands/*.md` 与 user-level 技能副本存在内容重复，编辑时要同步（以 workspace `commands/` 为源，改完让我重新同步到 user-level）。

## 两份 .doc 培训文档纳入知识库（ingest 实操）
- 源目录 `D:\培训文档\广告培训\` 有多份亚马逊广告培训文档（.doc/.docx/.pdf/.pptx/.xlsx）。
- 已收录两份到 L1 `00-Raw-Sources/articles/` 并做 L2 摘要+概念+级联更新：
  1. `【03运营】亚马逊广告预算的运作机制.doc` → 概念页 `广告预算机制`（每日预算=平均数/月度封顶/同日改预算取最高/分配公式/花不出去诊断）。
  2. `【04运营】亚马逊Rufus AI 新增一个收费广告位.doc` → 概念页 `Rufus AI 广告位`（Prompts 智购 SPP/SBP，2026-03-25 转 CPC，默认开启易非预期支出）。
- **关键坑（WPS .doc 转换）**：第 2 份是 WPS Office 生成的 OLE2(.doc)，`antiword -m UTF-8` 报 "is not a Word Document"（antiword 只认微软 OLE2 变体）。Word COM / KSO COM 被安全策略拦截；pandoc/libreoffice 均未安装。
  - **解法**：用 Python 标准库手写 OLE2 解析（读 FAT/Directory 链）→ 取 `WordDocument` 流 → 启发式抽取 UTF-16LE 连续文本（过滤 CJK/ASCII/常见标点，丢弃二进制的页脚噪点）。代码可复用，下次遇 WPS .doc 直接套。
  - 第 1 份是微软 OLE2，`antiword -m UTF-8 "D:/培训文档/...doc"`（**必须 Windows 风格路径 D:/...，不能用 /d/... 或 /tmp/...**）可直接提取。
- 提醒：用户首个 Rufus 请求打断了第 1 份的级联更新（task 4/5），后续一并补齐——两份的级联都更新了 Amazon PPC 基础/术语表/MOC-营销与广告/MOC-平台运营/平台运营综述/Amazon 平台，并给两个概念页加回链消除孤立摘要页。

## PPTX《站内广告优化策略与实践》纳入知识库（ingest 实操）
- L1 已就位：`00-Raw-Sources/articles/2026-07-25-站内广告优化策略与实践.pptx`（10 页，亚马逊广告流量漏斗四阶段：认知/考虑/转化/忠诚）。本次只读不写 L1。
- 新建 3 页：摘要 `2026-07-25-站内广告优化策略与实践-摘要.md`、核心概念 `广告漏斗.md`（四阶段+广告类型匹配矩阵+全漏斗优化循环+趋势）、`关键词匹配类型.md`（广泛/词组/精准匹配，按阶段选用）。
- 级联更新 8 页：Amazon PPC 基础(补 SBV/Stores/ROAS/匹配类型链接)、广告预算机制(新增"按漏斗阶段分配"小节，认知 20-30% 与类型 75:20:5 视作互补维度非冲突)、Rufus AI 广告位、MOC-营销与广告(加概念+摘要区)、MOC-平台运营、平台运营综述(全漏斗一句)、Amazon 平台、术语表(补 CPM/CTR/CVR/ROAS/LTV/再营销/视频广告/品牌商店/漏斗等)。
- 校验：全局 260 条 L2 wikilink 0 断链；新页均被 MOC/概念页回链，无孤立页。首页.md 无需改动。
- 结论：本 PPTX 与既有页**无数据冲突**（预算两维度互补、指标一致），未加 `> [!warning]`。
